<div class="content">
  <div class="title">
    <h2><i class="fas fa-sign-in-alt"></i> 管理者ログイン</h2>
    <%= render "admin/shared/error_messages", resource: resource %>
  </div>
  <div class="form-style">
    <%= form_with model: @admin, url: admin_session_path, local: true do |f| %>

      <!--メールアドレス-->
      <div class="field">
        <%= f.label :email, "メールアドレス" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "item" %>
      </div>

    　<!--パスワード-->
      <div class="field">
        <%= f.label :password, "パスワード" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "item" %>
      </div>

      <div class="actions">
        <%= f.submit "ログイン", class: "btn btn-green" %>
      </div>
    <% end %>

    <p class="pf-info">
      ※ ポートフォリオ閲覧用の管理者アカウント情報を下記に記載しております。<br>
      &emsp;&nbsp;コピー＆ペーストした後、上の入力欄に入力してください。
    </p>
    <div class="pf-account">
      <p>
        メールアドレス: admin@test.test<br>
        パスワード: 123456
      </p>
    </p>
  </div>
</div>

